<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Task Manager</title>
 <link rel="icon" type="image/png" href="/favicon.png" />
  <script src="https://cdn.tailwindcss.com"></script>
  <script src="https://cdn.lordicon.com/lordicon.js"></script>
</head>

<body
  class="bg-[radial-gradient(ellipse_at_top_left,_var(--tw-gradient-stops))] from-[#7bc74d] via-[#222831] to-[#393e46] text-white">
  <div class="min-h-screen w-full px-4 py-4 sm:px-8 md:px-16 lg:px-32">
    <!-- Header -->
    <div class="flex justify-center items-center">
      <div
        class="mb-10 text-center bg-[conic-gradient(at_top_left,_var(--tw-gradient-stops))] from-[#6366f1] via-[#9d174d] to-[#9d174d] w-full max-w-xl px-4 py-1 rounded-lg">
        <h1 class="text-2xl sm:text-3xl md:text-4xl font-bold text-white">📝 Task Manager</h1>
      </div>
    </div>


    <!-- Form Section -->
    <div class="bg-zinc-950/20 p-8 rounded-xl shadow-md max-w-xl mx-auto">
      <form action="/create" method="POST">
        <label class="block text-[18px] tracking-wider font-semibold mb-2 text-zinc-300">Title</label>
        <input type="text" name="title" placeholder="Enter task title" required
          class="block px-4 py-2 mb-4 w-full bg-zinc-700/40 border border-zinc-600 rounded-lg text-white placeholder-zinc-400 focus:outline-none focus:ring-1 focus:ring-green-600" />

        <label class="block text-[18px] tracking-wider mb-2 text-zinc-300">Details</label>
        <textarea name="details" placeholder="Describe your task" required rows="4"
          class="block px-4 py-2 mb-4 w-full bg-zinc-700/40 border border-zinc-600 rounded-lg text-white placeholder-zinc-400 focus:outline-none focus:ring-1 focus:ring-green-600 resize-none"></textarea>

        <button type="submit"
          class="w-full bg-gradient-to-tl from-blue-700 via-blue-800 to-gray-900 hover:bg-blue-700 transition duration-200 px-4 py-2.5 mt-2 rounded-lg font-semibold text-white">
          ➕ Add Task
        </button>
      </form>
    </div>

    <!-- Tasks Section -->
    <div class="mt-12 flex flex-wrap gap-6 justify-center">
      <% if (files.length> 0) { %>
        <% files.forEach(file=> { %>
          <div class="w-72 p-6 bg-zinc-800/70 rounded-xl shadow-md hover:shadow-lg transition duration-300">
            <h2
              class="text-xl font-bold text-transparent bg-clip-text bg-gradient-to-r from-pink-400 via-purple-400 to-indigo-500 text-center border border-purple-500 py-1 w-full rounded-md shadow-md hover:scale-105 transition-transform duration-200 truncate cursor-pointer">
              <%= file.replace('.txt', '' ) %>
            </h2>
            <div class="mt-8 flex flex-wrap justify-center items-center gap-x-8 sm:gap-x-4 gap-y-3">
              <a href="/file/<%= file %>"
                class="bg-blue-600 text-white px-4 py-2 text-sm sm:text-base rounded-md hover:bg-blue-700 transition duration-200 shadow-md">
                📖 Read
              </a>
              <a href="/delete/<%= file %>"
                class="bg-red-600 text-white px-4 py-2 text-sm sm:text-base rounded-md hover:bg-red-700 transition duration-200 shadow-md">
                🗑️ Delete
              </a>
            </div>

          </div>
          <% }) %>
            <% } else { %>
              <p class="text-zinc-400 text-center w-full">No tasks yet.</p>
              <% } %>
    </div>
  </div>
</body>

</html>